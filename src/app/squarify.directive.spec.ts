import { SquarifyDirective } from './squarify.directive';

describe('SquarifyDirective', () => {
  it('should create an instance', () => {
    const directive = new SquarifyDirective();
    expect(directive).toBeTruthy();
  });
});
